"""The web layer — a leaf. It imports the core; the core never imports it.

The rule is about the direction of the arrow, not the location. Enforce it with
an import-linter contract rather than a comment.
"""
