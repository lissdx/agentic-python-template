"""Command-line entry points.

A thin shell: parse arguments, build the objects, call into the package, print.
Logic that lives here cannot be imported or tested by anything else, so keep it
to wiring.
"""
